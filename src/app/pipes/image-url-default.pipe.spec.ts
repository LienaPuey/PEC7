import { ImageUrlDefaultPipe } from './image-url-default.pipe';

describe('ImageUrlDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageUrlDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
